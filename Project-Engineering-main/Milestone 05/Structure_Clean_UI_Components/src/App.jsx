import DashboardPage from "./pages/DashboardPage";

export default function App() {
  return <DashboardPage />;
}
// this is my app.jsx file, which serves as the entry point for my React application. It imports the DashboardPage component and renders it. The DashboardPage is the main container that holds all the state and logic for managing tasks. It uses several child components to render different parts of the UI, such as the header, stats, add task input, filter bar, and task list. Each child component is focused on a specific piece of functionality, making the code more modular and easier to maintain.